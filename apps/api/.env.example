# Copy to .env to override Compose defaults. `docker compose up` works without a .env.

POSTGRES_USER=laplaza
POSTGRES_PASSWORD=laplaza
POSTGRES_DB=laplaza_development
POSTGRES_PORT=5432
API_PORT=3000
